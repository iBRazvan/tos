import React from "react";
import styles, { layout } from "../style";

const Services = () => {
  return (
    <section>
      x<h1 className={`${styles.heroHeader} text-white`}>Serviciile noastre</h1>
      <div className={`${styles.boxWidth}${styles.colContainer} `}></div>
    </section>
  );
};

export default Services;
